# ADR 0006: REST is the v1 datasource

Status: accepted

Version 1 implements ERS, Open API, and MnT REST adapters. Data Connect and
pxGrid extend the same registry after the REST contract is proven.
